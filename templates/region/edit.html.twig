{% extends 'base.html.twig' %}

{% block title %}Edit Region{% endblock %}

{% block body %}
<div class="container bg-light rounded mt-3 p-2" style="width: 600px">
    <div class="d-flex justify-content-center ">
        <h1 class="text-danger">Edit Region</h1>
    </div>
<div class="container-fluid" style="width: 500px;">
    {{ include('region/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary mt-2" href="{{ path('region_index') }}">back to list</a>

    {{ include('region/_delete_form.html.twig') }}
</div>
{% endblock %}
