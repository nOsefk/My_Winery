{% extends 'base.html.twig' %}

{% block title %}New Region{% endblock %}

{% block body %}
<div class="container bg-light rounded mt-3 p-2" style="width: 600px">
    <div class="d-flex justify-content-center">
        <h1 class="text-danger">Create new Region</h1>
    </div>
    {{ include('region/_form.html.twig') }}

    <a class="btn btn-primary mt-3" href="{{ path('region_index') }}">Back to list</a>
</div>
{% endblock %}
